{% macro footer(categoryAlias) -%}
    <footer class="entry-meta">
		{{ postedIn }}<a href="{{getCategoryUrlByAlias(categoryAlias)}}"
						 title="Просмотреть все заметки в {{getCategoryNameByAlias(categoryAlias)}}"
						 rel="category tag">{{getCategoryNameByAlias(categoryAlias)}}</a>
	</footer><!-- .entry-meta -->
{%- endmacro %}